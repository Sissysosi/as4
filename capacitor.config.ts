import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'as4mwe',
  appName: 'donate2nation',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
